# B1 委派提示词 · 分支集成到 main（可直接复制给子 agent）

> ⚠️ **部分已被取代（2026-09-13）**：第 1、2 段（A rbac / B sport-type）**已执行完毕并合入 main**（main 尖端 `b8064a3`），无需再派。
> 第 3、4 段（C lockout / D 收尾）已被 **`docs/委派提示词-剩余4段-合并版.md` 的 P1** 取代。
> **以新文件为准**，本文件仅作历史留档，避免口径漂移。

> **使用说明**：本文件含 4 段提示词，**必须按 A → B → C → D 顺序、一段一段单独派发**，不要合并成一段丢给同一个子 agent 一次性做完。
> 理由：每次合并都会移动 `main`，后一段的校验基线依赖前一段的结果；一次性做完会失去「哪一步把 main 弄坏了」的定位能力。
>
> **派发前你先做一件事**：确认工作区干净度。当前工作区有 4 个未跟踪项（3 个提案目录 + 交接文档）——**这是预期的，不要清理**，提示词 D 会正式提交它们。
>
> **配套文档**：`docs/项目指导-后续任务与风险指导意见.md`（第 1 节是勘误依据，第 7 节是可用命令）。

---

## 派发前的基线事实（供你判断子 agent 是否说真话）

| 项 | 实测值 |
| --- | --- |
| 全仓 commit | 62（7 个分支） |
| `main` 尖端 | `154e2ce merge: add-jwt-auth 合入主线`（53 commit） |
| 未合入 main | `feature/add-admin-rbac`(4 ahead/0 behind)、`feature/add-sport-type-threshold`(9/0，**叠在 rbac 之上**)、`feature/add-login-lockout`(4/1)、`feature/add-two-level-cache`(4/9，**归 B4，本批不动**) |
| 冲突预演 | rbac→main **0 冲突**；sport-type→main **0 冲突**；lockout→main **0 硬冲突**，但 4 个文件需人工过目（见提示词 C） |
| 编译 | `clean install -DskipTests` BUILD SUCCESS，8 模块 |
| 测试 | `mvn test` BUILD SUCCESS，108 用例 |
| 未跟踪项 | `spec/changes/add-admin-rbac/`、`spec/changes/add-login-lockout/`、`spec/changes/add-sport-type-threshold/`、`docs/项目指导agent交接文档.md` |

> **合并顺序的硬约束**：A（rbac）必须在 B（sport-type）之前。因为 sport-type 分支**叠在** rbac 分支之上，先合 rbac 后，sport-type 的合并就是干净的快进式增量；顺序颠倒会把 rbac 的 4 个 commit 裹进 sport-type 的合并里，`git branch --merged main` 会残留 rbac 未合并的假象。

---

## 提示词 A —— 合并 `feature/add-admin-rbac`（治理面 RBAC）

```
【任务】把 feature/add-admin-rbac 合并进 main，并为其补交提案文档。

【只允许写哪些文件】
- 允许：新增/提交 spec/changes/add-admin-rbac/ 下的全部文件（proposal.md、tasks.json、specs/sport-record-verify/spec-delta.md）
- 允许：git 分支操作（checkout / add / commit / merge）
- 禁止：改动任何 .java、.yml、.properties、.sql、README.md 内容
- 禁止：触碰 feature/add-sport-type-threshold、feature/add-login-lockout、feature/add-two-level-cache 三条分支
- 声明：除本任务列出的文件外，无其他写入者。若发现需要改其他文件，停下来上报，不要自作主张。

【必读材料（先读再动手）】
1. spec/changes/add-admin-rbac/proposal.md（该变更的 Why / What / Impact）
2. spec/changes/add-admin-rbac/tasks.json（4 个 task，注意 status 字段全为 false，本次不要回写）
3. docs/项目指导-后续任务与风险指导意见.md 第 1 节勘误 1（分支现状）与第 7 节（命令勘误）

【执行步骤】
1. 先记录基线：git log --oneline -1 main、git status --short。把结果写进回报。
2. 关键一步：spec/changes/add-admin-rbac/ 目前是**未跟踪文件**，只存在于工作区。
   先把它提交到**它自己的分支**上，这样合并时提案会随代码一起进 main：
     git checkout feature/add-admin-rbac
     git add spec/changes/add-admin-rbac
     git commit -m "docs(rbac): 提案与规范差异入库"
3. 合并进 main：
     git checkout main
     git merge --no-ff feature/add-admin-rbac
   预期零冲突（已用 merge-tree 预演确认）。若真出现冲突，**最多尝试修复 1 次**；仍不通过就 git merge --abort 并如实上报，不要循环折腾。
4. 校验（必须贴输出摘要）：
     git log --oneline -3
     git branch --merged main | grep rbac
     （编译与测试由提示词 D 统一跑，本步不跑，避免重复耗时）

【验收项】
- main 尖端是本次的合并提交
- feature/add-admin-rbac 出现在 git branch --merged main 中
- spec/changes/add-admin-rbac/ 三个文件已进入 git 跟踪（git ls-files spec/changes/add-admin-rbac 有 3 条）
- 未改动任何业务代码文件

【commit 计划】
本任务只产生 1 个 commit（步骤 2 的 docs(rbac)），合并提交由 git 自动生成。

【回报格式（≤300 字，不要贴代码）】
- 产出：涉及的分支名 / commit hash
- 校验：上述 4 条验收项的实测结果（命令 + 一句话结论）
- 未解决项
- 待决策项
```

---

## 提示词 B —— 合并 `feature/add-sport-type-threshold`（阈值分运动类型）

```
【任务】把 feature/add-sport-type-threshold 合并进 main（前置：提示词 A 已完成，main 已含 rbac）。

【只允许写哪些文件】
- 允许：新增/提交 spec/changes/add-sport-type-threshold/ 下的全部文件
- 允许：git 分支操作
- 禁止：改动任何 .java、.yml、.properties、.sql 内容
- 禁止：触碰 feature/add-login-lockout、feature/add-two-level-cache（后者归 B4，明确不在本批）
- 声明：无其他写入者。需越界改动时停下来上报。

【必读材料】
1. spec/changes/add-sport-type-threshold/proposal.md（阈值分类型的 Why：骑行正常速度被 R1 5.5m/s 误杀）
2. spec/changes/add-sport-type-threshold/tasks.json
3. docs/项目指导-后续任务与风险指导意见.md 第 1 节勘误 1

【关键现状（不要重复造轮子）】
- 该分支已含 9 个 commit = rbac 的 4 个 + 自己的 5 个（**它是叠在 rbac 之上的**）。A 完成后，本步只剩 5 个 commit 需要进 main。
- 实现已完成：record 侧 SportRecord/DTO 加 sportType、verify 侧阈值结构按类型分维度、VerifyEngine 按 sportType 取阈值、rules_json 类型嵌套与向后兼容。**不要重写，只做合并。**

【执行步骤】
1. 基线：git log --oneline -1 main（应已是 A 的合并提交）
2. 提交提案文档（同样是未跟踪文件，必须在它自己的分支上提交）：
     git checkout feature/add-sport-type-threshold
     git add spec/changes/add-sport-type-threshold
     git commit -m "docs(sport-type): 提案与规范差异入库"
3. 合并：
     git checkout main
     git merge --no-ff feature/add-sport-type-threshold
   预期零冲突（已预演）。冲突处理同上：最多修 1 次，否则 abort + 上报。
4. 校验：
     git log --oneline -3
     git branch --merged main | grep -E "rbac|sport-type"   # 两条都应在列
     git show main:README.md | grep -c "运动类型阈值分级"      # 期望 ≥1 且位于「变更交付」段而非「后续变更（待办）」段

【验收项】
- main 尖端是本次合并提交
- rbac 与 sport-type 两条分支均在 git branch --merged main 中
- main 的 README 里「运动类型阈值分级」从**待办清单**移到了**变更交付**段（这点必须贴出前后两行原文，它是勘误 1 的现场证据）
- spec/changes/add-sport-type-threshold/ 已进入 git 跟踪
- 未改动任何业务代码文件

【commit 计划】
1 个 commit（docs(sport-type)）+ git 自动生成的合并提交。

【回报格式（≤300 字）】
- 产出：分支 / commit hash
- 校验：4 条验收项实测结果（README 那段请贴原文两行）
- 未解决项
- 待决策项
```

---

## 提示词 C —— 合并 `feature/add-login-lockout`（账号锁定）

```
【任务】把 feature/add-login-lockout 合并进 main（前置：A、B 已完成）。

【只允许写哪些文件】
- 允许：新增/提交 spec/changes/add-login-lockout/ 下的全部文件
- 允许：git 分支操作
- 允许（**仅限合并冲突时**）：user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java、docs/adr/0007-鉴权设计.md、docs/项目速览手册.md、README.md —— 这 4 个文件是合并的交叉面，冲突时按下面的口径取舍
- 禁止：触碰 feature/add-two-level-cache
- 声明：无其他写入者。

【必读材料】
1. spec/changes/add-login-lockout/proposal.md（Why：失败计数已做，但达阈值只 log.warn、照样校验密码 → 撞库没有成本）
2. spec/changes/add-login-lockout/tasks.json
3. docs/项目指导-后续任务与风险指导意见.md 第 1 节**勘误 3** ← 必读，它解释了「为什么这条变更必须合」

【关键现状（这条变更的意义，别搞错重点）】
- main 上 `AuthService` 目前只有「失败计数 + 阈值告警」，注释明写「锁定落地属后续变更」——也就是说 main 上的「登录爆破防护」是**讲设计**状态。
- 本分支实现在未合并分支上：`auth:lock:{phone}` 锁定键（TTL=锁定时长）、登录入口前置锁定检查（**不校验密码、不消耗 BCrypt**）、成功登录同时清计数键与锁定键、`app.auth.lock.*` 可配、`AuthServiceTest` 188 行单测。
- 合并后，审批版提到的「失败锁定」才在 main 上成立。**这正是勘误 3 要解决的口径矛盾。**

【执行步骤】
1. 基线：git log --oneline -1 main
2. 提交提案文档（未跟踪文件，在自己的分支上提交）：
     git checkout feature/add-login-lockout
     git add spec/changes/add-login-lockout
     git commit -m "docs(lockout): 提案与规范差异入库"
3. 合并：
     git checkout main
     git merge --no-ff feature/add-login-lockout
   **预期**：0 处硬冲突标记，但有 **4 个文件**两侧都改过，git 会自动合并 —— 自动合并**不等于**语义正确，必须逐个打开过目（这是本步唯一需要动脑的地方）：
   - `README.md`、`docs/项目速览手册.md`、`docs/adr/0007-鉴权设计.md`：确认合并后**同时**保留了 jwt 的内容与 lockout 的新增段落，没有把已交付的鉴权描述覆盖回旧版。
   - `AuthService.java`：确认锁定前置检查、达阈值写锁定键、成功清锁定三处逻辑都在，且与 main 上已有的失败计数逻辑不重复、不打架。
4. 冲突/异常处理：**最多修 1 次**；仍不通过就 `git merge --abort` 并如实上报。不要为「让它能过」而删减功能代码。
5. 校验：
     git log --oneline -3
     git branch --merged main | grep -E "rbac|sport-type|lockout"     # 三条都应在列
     grep -c "auth:lock:" user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java   # 期望 ≥1
     grep -c "锁定落地属后续变更" user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java  # 期望 0（旧口径注释应已被新实现取代）
     ls user-service/src/test/java/com/sportverify/user/auth/service/AuthServiceTest.java   # 应存在

【验收项】
- 三条分支全部 merged 进 main
- main 上 AuthService 含 `auth:lock:` 锁定逻辑；旧的「锁定落地属后续变更」注释不再残留
- AuthServiceTest 已随分支进入 main
- 四个交叉文件人工过目结论已写进回报（各一句话）
- git status --short 中该 3 个提案目录不再是未跟踪状态

【commit 计划】
1 个 commit（docs(lockout)）+ git 自动生成的合并提交。交叉文件的冲突取舍若产生了改动，单独一个 commit：fix(lockout): 合并交叉文件取舍。

【回报格式（≤300 字）】
- 产出：分支 / commit hash
- 校验：5 条验收项实测结果 + 4 个交叉文件各一句过目结论
- 未解决项
- 待决策项
```

---

## 提示词 D —— B1 收尾：全量校验 + 提交项目文档

```
【任务】B1 收尾：在 main 上跑全量编译与测试，并提交两份项目级文档。

【前置】提示词 A/B/C 全部完成，main 已含 rbac、sport-type、lockout。

【只允许写哪些文件】
- 允许：提交 docs/项目指导agent交接文档.md、docs/项目指导-后续任务与风险指导意见.md
- 允许：git 分支/提交操作
- 禁止：改动任何业务代码、配置、规范文件
- 声明：无其他写入者。

【执行步骤】
1. 确认在 main 上、工作区除待提交文档外无其他改动：git status --short
2. 编译（**注意：Git Bash 直接 mvn 会报「找不到主类 classworlds.launcher」，因为 PATH 上的 java 是 1.8。必须用 JDK21 显式启动**）：
     MVN=/d/develop/Maven/apache-maven-3.9.4
     JH=/d/develop1/jdk21
     "$JH/bin/java" -classpath "$(cygpath -w $MVN/boot/plexus-classworlds-2.7.0.jar)" \
       "-Dclassworlds.conf=$(cygpath -w $MVN/bin/m2.conf)" "-Dmaven.home=$(cygpath -w $MVN)" \
       -Dmaven.multiModuleProjectDirectory="D:\code\sports" \
       org.codehaus.plexus.classworlds.launcher.Launcher -B clean install -DskipTests
3. 测试（同一条命令把目标换成 test）：
     期望 BUILD SUCCESS，且用例总数 >= 108（lockout 分支带来 188 行 AuthServiceTest，总数应上升）
4. 提交文档：
     git add docs/项目指导agent交接文档.md docs/项目指导-后续任务与风险指导意见.md
     git commit -m "docs: 项目指导交接文档与第2期指导意见"
5. 终检：
     git status --short          # 期望：干净
     git branch --no-merged main # 期望：只剩 feature/add-two-level-cache（它归 B4）
     git log --oneline -8

【验收项】
- 编译 BUILD SUCCESS（贴模块汇总那几行）
- 测试 BUILD SUCCESS，0 失败 0 错误，用例数 >= 108（贴用例总数那行）
- git status --short 干净
- git branch --no-merged main 仅剩 feature/add-two-level-cache
- 两份文档已入库（git ls-files | grep 项目指导 有 2 条）

【不在本任务范围内（明确不做，避免越界）】
- 不跑压测、不做服务冒烟（这两项由用户亲自执行）
- 不合 feature/add-two-level-cache（归 B4，需连带做 L2 序列化往返断言）
- 不改 spec/specs/.../spec.md 主规范（规范归档归 B3）

【回报格式（≤300 字）】
- 产出：commit hash / 用例总数
- 校验：5 条验收项实测结果（编译与测试各贴关键 1-3 行）
- 未解决项
- 待决策项

【回报里必须附一句给用户的话】
「main 已集成完毕，请你自己跑一次冒烟」——并列出 3 条冒烟命令（榜单新路径、鉴权 /admin/** 403、mapmatch R5 命中/不命中）。AI 的测试不能替代真机冒烟。
```

---

## B1 完成后，交给你的 3 条人工冒烟（子 agent 不能替代）

合并后必须**你亲自**跑，因为「服务能不能起来」只有真机能盖章：

```bash
# 1. 中间件必须全起（实测 Nacos:8848 与 RocketMQ:9876 常未运行，冒烟失败多半是这里）
docker compose up -d
netstat -ano | grep LISTENING | grep -E ":(8848|9876|3306|6379|5432)\b"

# 2. 起 6 服务：8080/8081/8082/8083/8084/8085

# 3. 冒烟一：榜单（破坏性变更后的新路径）
curl http://127.0.0.1:8080/leaderboard/api/leaderboard?type=overall

# 4. 冒烟二：鉴权（B1 之后这条才应该通过；合之前必失败，那正是勘误 1 的现场证据）
#    无 token → 401(1001)；普通用户 token 访问 /admin/** → 403(1002)；ADMIN token → 放行

# 5. 冒烟三：账号锁定（本次新合入的能力）
#    同一手机号连错 5 次密码 → 第 6 次直接返回锁定错误，且不消耗 BCrypt；登录成功即清零

# 6. 冒烟四：空间匹配（R5）
#    真实沿路轨迹 → R5 不命中；悬浮轨迹 → R5 命中

# 7. Redis 核对（记得 host.docker.internal，见坑 2）
docker exec sport-verify-redis redis-cli -h host.docker.internal -p 6379 KEYS 'leaderboard:*'
```

---

## 我在 B1 之后立刻接手的动作（你无需派发）

1. **B4 提示词**：二级缓存 rebase + L2 序列化往返断言（这是全项目风险最高的一处，我会把「静默降级」的验证方法写成可执行断言用例，再用四件套格式给你）。
2. **B3 委派提示词 ×5**：规范归档（jwt / mapmatch / rbac / sport-type / lockout），每条含必读的 spec-delta、主规范插入位置、以及 mapmatch 那条 **MODIFIED 段的特判**（已核实是「服务划分」需求 5→6，要连需求正文 + `> 变更说明` 引用块 + 场景描述三处一起改，不是纯追加）。
3. **口径复核**：`docs/项目速览手册.md`、README、`docs/简历项目描述模板.md` 里「二级缓存」「账号锁定」的表述，在 B4/B1 合并完成后逐一核对，确保与实现一致（诚实原则）。
