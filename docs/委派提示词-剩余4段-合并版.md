# 剩余批次 · 委派提示词（合并版，4 段）

> **本文件取代**（避免两个版本口径漂移）：
> - `docs/委派提示词-B1-分支集成.md` 的**第 3、4 段**（C lockout / D 收尾）→ 由本文件 **P1** 取代
> - `docs/委派提示词-B3-规范归档.md` 全部 → 由本文件 **P3**（① - ⑤）与 **P4 第 8 步**（⑥ cache）取代
> - `docs/委派提示词-B4-二级缓存集成.md` → 由本文件 **P4** 取代
>
> **合并依据（2026-09-13 实测，非假设）**：
> | 项 | 实测 |
> | --- | --- |
> | 已完成 | `feature/add-admin-rbac`、`feature/add-sport-type-threshold` **均已合入 main**，main 尖端 `b8064a3` |
> | lockout → main | **0 处硬冲突**；4 个文件两侧都改过（README.md、docs/adr/0007-鉴权设计.md、docs/项目速览手册.md、AuthService.java） |
> | cache → main | **0 处硬冲突**；2 个文件两侧都改过（docs/项目速览手册.md、verify-service/.../application.yml） |
> | 结论 | 两条都**不需要 rebase**，直接 `--no-ff` merge 即可；但「无冲突」≠「能编译」≠「语义正确」 |
>
> **派发顺序**：P1 → P2 → P3 → P4（P4 必须在 P3 之后：P4 第 8 步要归档 cache 提案，P3 做的是其余 5 个）

---

## 全局纪律（4 段通用）

1. **四件套**：只允许写指定文件 + 最多修复 1 次 + 回报 ≤300 字 + 细节留文件里。
2. **commit 纪律**：一个 step 一个 commit，conventional commits，body 中文「改了什么：为什么」，**commit 之间保持可独立编译**。
3. **诚实纪律**：服务冒烟与压测仅由用户执行，AI 不得声称已通过；规范措辞只陈述实现事实，不拔高。
4. **环境**：Maven 必须用 JDK21 显式启动（Git Bash 直接 `mvn` 会报「找不到主类 org.codehaus.plexus.classworlds.launcher.Launcher」）。

---

## P1 —— 合并 lockout + B1 收尾（全量编译测试 + 文档入库）

```
【任务】合并 feature/add-login-lockout 进 main，并做 B1 收尾：全量编译测试 + 提交未跟踪的项目文档。

【前置】main 已含 rbac 与 sport-type（尖端 b8064a3）

【只允许写哪些文件】
- 允许：新增/提交 spec/changes/add-login-lockout/ 下的全部文件
- 允许：.gitignore（仅追加一行 .workbuddy/）
- 允许：提交 docs/项目指导agent交接文档.md、docs/项目指导-后续任务与风险指导意见.md、docs/委派提示词-*.md
- 允许（仅限合并交叉时）：user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java、
  docs/adr/0007-鉴权设计.md、docs/项目速览手册.md、README.md
- 禁止：改动其他任何业务代码、配置、规范文件
- 禁止：触碰 feature/add-two-level-cache
- 声明：无其他写入者。需越界时停下来上报。

【必读材料】
1. spec/changes/add-login-lockout/proposal.md（Why：失败计数已做，但达阈值只 log.warn、照样校验密码 → 撞库无成本）
2. spec/changes/add-login-lockout/tasks.json
3. docs/项目指导-后续任务与风险指导意见.md 第 1 节勘误 3 —— 它解释了「为什么这条变更必须合」

【关键现状（实测）】
- 该分支对 main **0 处硬冲突**，但有 4 个文件两侧都改过：README.md、docs/adr/0007-鉴权设计.md、
  docs/项目速览手册.md、AuthService.java。git 会自动合并，**但自动合并不等于语义正确**，必须逐个打开过目。
- main 上 AuthService 目前只有「失败计数 + 阈值告警」，注释写着「锁定落地属后续变更」；本分支带来
  auth:lock:{phone} 锁定键（TTL=锁定时长）、登录入口前置检查（不校验密码、不消耗 BCrypt）、成功登录清锁定、
  app.auth.lock.* 配置、AuthServiceTest（4 个 @Test）。
- 当前测试基线：108 用例。合并后应为 >=112。

【执行步骤】
1. 基线：git log --oneline -1 main；git status --short（记下未跟踪项，稍后要提交）
2. 提交提案目录（它只在工作区、未跟踪，必须在它自己的分支上提交，否则合并带不进 main）：
     git checkout feature/add-login-lockout
     git add spec/changes/add-login-lockout
     git commit -m "docs(lockout): 提案与规范差异入库"
3. 合并（0 硬冲突，可 --no-ff；不要 rebase）：
     git checkout main
     git merge --no-ff feature/add-login-lockout
   **最多修复尝试 1 次**；仍不通过就 git merge --abort 并如实上报。不要为「让它能过」而删减功能代码。
4. 逐个过目 4 个交叉文件（本步唯一需要动脑的地方，每份给一句结论）：
   - README.md、docs/项目速览手册.md、docs/adr/0007-鉴权设计.md：确认合并后**同时**保留 jwt 的内容与 lockout 的新增段落，
     没有把已交付的鉴权描述覆盖回旧版。
   - AuthService.java：确认锁定前置检查、达阈值写锁定键、成功清锁定三处逻辑都在，且与 main 上已有的
     失败计数逻辑不重复、不打架。
5. 全量编译 + 测试（Maven 必须用 JDK21 显式启动）：
     MVN=/d/develop/Maven/apache-maven-3.9.4
     JH=/d/develop1/jdk21
     "$JH/bin/java" -classpath "$(cygpath -w $MVN/boot/plexus-classworlds-2.7.0.jar)" \
       "-Dclassworlds.conf=$(cygpath -w $MVN/bin/m2.conf)" "-Dmaven.home=$(cygpath -w $MVN)" \
       -Dmaven.multiModuleProjectDirectory="D:\code\sports" \
       org.codehaus.plexus.classworlds.launcher.Launcher -B clean install -DskipTests
     # 再跑测试：把目标换成 test
     期望 BUILD SUCCESS，用例总数 >=112，0 失败 0 错误
6. .gitignore 追加一行 .workbuddy/（agent 工作记忆，不入公开仓库）：
     git add .gitignore && git commit -m "chore: .gitignore 忽略 .workbuddy 工作记忆目录"
7. 提交项目文档：
     git add docs/项目指导agent交接文档.md docs/项目指导-后续任务与风险指导意见.md docs/委派提示词-*.md
     git commit -m "docs: 项目指导与委派提示词入库"
8. 终检：
     git status --short          # 期望：干净，未跟踪项清零
     git branch --no-merged main # 期望：只剩 feature/add-two-level-cache
     git log --oneline -6

【验收项】
1. feature/add-login-lockout 出现在 git branch --merged main
2. main 上 AuthService 含 auth:lock:（grep -c >=1），且「锁定落地属后续变更」注释已消失（grep -c = 0）
3. user-service/.../auth/service/AuthServiceTest.java 已存在于 main
4. 编译 BUILD SUCCESS + 测试 BUILD SUCCESS，用例数 >=112（贴用例总数那行）
5. 4 个交叉文件各一句过目结论
6. git status --short 干净（未跟踪项清零）；.workbuddy/ 已被忽略
7. git branch --no-merged main 仅剩 feature/add-two-level-cache

【明确不做】不合 cache 分支（归 P4）；不做规范归档（归 P3）；不跑压测、不做服务冒烟

【commit 计划】
- docs(lockout): 提案与规范差异入库
- 合并提交（git 自动生成）
- fix(lockout): 合并交叉文件取舍（若有实际改动才建）
- chore: .gitignore 忽略 .workbuddy 工作记忆目录
- docs: 项目指导与委派提示词入库

【回报格式（≤300 字，不贴代码）】
- 产出：commit hash 列表 / 用例总数
- 校验：7 条验收项实测结果（编译与测试各贴关键 1-3 行；第 5 条贴 4 句结论）
- 未解决项 / 待决策项
- 必附一句：「main 已集成完毕，请你自己跑一次冒烟」+ 列出冒烟命令
  （榜单新路径 /leaderboard/api/leaderboard、鉴权 /admin/** 403、账号锁定连错 5 次被拒、mapmatch R5 命中与不命中）
```

---

## P2 —— GitHub 建仓首推

```
【任务】为 D:\code\sports 创建 GitHub 公开仓库并完成首次推送。

【允许】git remote / push、gh 命令；README.md 仅加一句口径说明；.gitignore 如需补充忽略项。
【禁止】改动业务代码。

【前置复核】
1. git ls-files | grep -iE '\.(env|pem|key|p12)$|target/|\.log$'   # 期望空
2. git status --short                                              # 期望干净（P1 已清零）
3. git ls-files | grep -c "scripts/mapmatch/data/\|docs/perf/data/.*jsonl"   # 期望 0

【步骤】
1. 仓库卫生确认：.git 约 3.9M、最大跟踪文件约 64K（健康）
2. README 顶层「快速开始」附近加一句：
   「配置文件中的数据库口令与 JWT 默认密钥仅用于本地 demo，生产请用环境变量注入」
   git add README.md && git commit -m "docs: 说明本地 demo 默认口令与密钥的口径"
3. 建仓并推送（gh 已登录则直接建；未登录则停下上报，不要尝试输入凭据）：
     git remote add origin git@github.com:<账号>/sport-record-verify.git
     git branch -M main
     git push -u origin main
     git push origin --all      # 如需保留分支
4. 校验：git remote -v 有值；gh repo view --json isPrivate,url 确认 public；
   网页可见 README、docs/adr/0001-0008、spec/specs/sport-record-verify/spec.md

【验收项】
- 远端存在且为 public（贴 gh 输出）
- README 口径句已提交
- 无敏感文件、无 target/、无路网与压测生成物入库
- 推送后 git status --short 干净

【失败处理】gh 未登录 / SSH key 未配置 → **停下上报**，不要循环重试、不要改用 HTTPS 口令

【回报格式（≤300 字）】仓库 URL / 推送的 commit 数 / 逐条前置复核结果 / 未解决项 / 待决策项
```

---

## P3 —— 规范归档 ×5（一条提示词，内部 5 个待办）

```
【任务】把 5 个已实施提案的 spec-delta 合并进主规范并归档。逐个做、逐个 commit。

【顺序（不可乱）】① add-jwt-auth → ② add-mapmatch-service → ③ add-admin-rbac
                → ④ add-sport-type-threshold → ⑤ add-login-lockout
  理由：③ 的需求要并入 ① 建立的 「## 鉴权」段落（同域不新开段落），所以 ③ 必须在 ① 之后。

【只允许写哪些文件】
- spec/specs/sport-record-verify/spec.md
- spec/changes/<各提案>/tasks.json（仅回写 steps[].completed 与 passes）
- git mv spec/changes/<提案> → spec/changes/archive/<提案>
- 禁止：改动任何 .java、.yml、README、docs/adr
- 声明：无其他写入者。

【机械规则（5 条通用，逐条都适用）】
R1 主规范只有三处改动位置：顶部「## 本规范已归档以下提案」清单追加一行 → 按能力域新增 `## <域>` 段落
   （放在末尾「## 变更历史」之前）→ 「## 变更历史」追加一条 bullet（格式 `- **<change-id>**：...`，引用对应 ADR）
R2 ADDED = 需求整段搬入（保持 ### Requirement: / #### Scenario: / GIVEN-WHEN-THEN-AND 原文不改写）；
   MODIFIED = 改写主规范里那条既有需求。本批只有 ② 带 MODIFIED。
R3 归档动作 = git mv 到 spec/changes/archive/ + 把该提案 tasks.json 里每个 task 的 steps[].completed 与 passes 回写 true。
   口径：「已归档」= 代码在 main + 编译测试绿 + 目录已移入 archive + spec-delta 已合并，四项缺一不许标 true。
R4 措辞纪律：需求原文不改写；确需改措辞先停下问我。规范只陈述实现事实，不拔高。
R5 允许的例外：② 的「服务划分」必须三处一起改（见 ② 细则）。

【逐条清单】
① add-jwt-auth（6 需求 / 13 场景，纯 ADDED）
   → 新增 `## 鉴权`；需求：用户注册、用户登录、Token 刷新与轮换、网关统一鉴权、用户数据隔离、鉴权降级开关
   → 变更历史引用 docs/adr/0007：双 token + refresh rotation（Redis+Lua）、网关统一鉴权、X-User-Id 数据隔离、
     越权 403、auth.enabled 降级开关
② add-mapmatch-service（6 需求 / 10 场景，1 段 ADDED + 1 段 MODIFIED）
   → 新增 `## 空间匹配`，搬入 6 条 ADDED（独立路网匹配服务、真实路网数据、R5 离路规则、判定聚合兼容、匹配降级）
   → MODIFIED 目标是「服务划分」需求本身，**不是「规则链判定（R1-R4）」**。R5 以 ADDED 新需求并入，R1-R4 那条不动。
     主规范「服务划分」现在写 5 个服务，并带引用块「> 变更说明：本需求由提案 add-leaderboard-service 的
     MODIFIED「服务划分」追加而来…服务数 4→5。」以及场景「THEN 可见 5 个服务各自注册 / AND 榜单职责不在 record-service 内」。
     **三处一起改**：需求正文改 6 个服务、引用块改写为「由 add-leaderboard-service 与 add-mapmatch-service
     两次 MODIFIED 演进：4→5→6」、场景改为「可见 6 个服务各自注册 / AND 空间匹配职责在 mapmatch-service 不在 verify-service」
   → 变更历史引用 docs/adr/0006：规则链 + 空间真实性双保险、R5 是首个依赖外部服务的规则（验证 Rule 接口可扩展性）、
     OSM 由「讲设计」升级为实锤
③ add-admin-rbac（4 需求 / 8 场景，纯 ADDED）
   → **前置校验**：git branch --merged main | grep rbac（不在列表则跳过并上报）
   → 4 条需求**并入 ① 的 `## 鉴权` 段落**（不新开段落），排在 jwt 需求之后
   → 变更历史引用 docs/adr/0007，写清与 ① 的分工：jwt 管业务面认身份，本变更管治理面授权（能改规则、能翻案）：
     USER/ADMIN 最小角色模型、access token 携带 role claim、`/admin/**` 与规则版本接口仅 ADMIN 可达
     （普通用户 403/1002、未登录 401/1001）、白名单从「裸放行」改为「进链校验角色」、app.auth.admin.enabled 默认启用
④ add-sport-type-threshold（4 需求 / 7 场景，纯 ADDED）
   → 前置校验：git branch --merged main | grep sport-type
   → 新增 `## 阈值分类型` 段落，或并入 `## 校验引擎`（你判断哪个更贴既有结构，选完在回报里说明理由）
   → 语义要点（必须写进变更历史）：未知/缺失类型保守回退 RUNNING 与历史行为一致；阈值分类型与灰度路由正交
     （灰度按 userId%100 路由版本，版本快照内部再按类型分维度）；rules_json 嵌套向后兼容（旧快照仍可解析）
   → **禁止**把「规则链判定（R1-R4）」改成 R1-R5
⑤ add-login-lockout（3 需求 / 6 场景，纯 ADDED）
   → 前置校验：git branch --merged main | grep lockout；
     grep -c "auth:lock:" user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java（期望 >=1，为 0 则跳过上报）
   → 新增 `## 账号锁定` 段落，搬入 3 条 ADDED（失败计数、达阈值锁定、自动解锁与成功清零）
   → 变更历史**必须含一句口径更正说明**：本需求此前长期处于「讲设计」状态（审批版列为能力项、实现只到计数+告警），
     本次由 add-login-lockout 在代码与规范两侧同时收口。这正是勘误 3 要解决的口径矛盾。
   → 措辞只陈述实现事实（「达阈值后拒绝登录，不校验密码、不消耗 BCrypt」），不要写成「自适应风控」之类的拔高表述

【进度自检（每做完一条贴一次累计值）】
起步：54 需求 / 120 场景
① 60/133 → ② 66/142或143 → ③ 70/150或151 → ④ 74/157或158 → ⑤ 77/163或164
说明：② 的「服务数」场景是**改写**而非新增，场景数可能比纯累加少 1，以实际为准并在回报里说明。

【验收项】
- 5 个提案全部移入 spec/changes/archive/，tasks.json 的 completed 与 passes 全为 true
- 主规范需求 77、场景 163±1（贴前后数值）
- 归档清单 +5 条、「变更历史」+5 条，排版与既有条目一致
- 「服务划分」三处一致为 6 服务（贴三处原文）
- 「规则链判定（R1-R4）」未被改动（贴证据）
- 变更历史含 lockout 的口径更正说明（贴原文）
- 除规范与归档动作外其他文件零改动（git status 佐证）

【失败处理】
某一条卡住 → 最多修 1 次；仍不通过就**跳过该条、在回报里标注原因，继续下一条**。
不要整体 abort，也不要回滚已完成的条目（每条一个 commit，本来就互不影响）。

【commit 计划】5 个 commit，每条一个：docs(spec): 归档 <change-id> 并合并 <能力域>

【回报格式（≤300 字，不贴需求原文）】
- 逐条一行：id / 需求场景增量 / commit hash / 状态（完成 或 跳过+原因）
- 再附 3 句：「服务划分」三处一致性摘要；「R1-R4 未动」的证据；lockout 变更历史原文那一句
- 未解决项 / 待决策项
```

---

## P4 —— 二级缓存合并 + L2 往返断言 + 归档 cache

```
【任务】把 feature/add-two-level-cache 合并进 main，补 L2 序列化往返断言测试，并把该提案归档。

【前置】P1、P3 已完成（main 编译测试绿；其余 5 个提案已归档）

【只允许写哪些文件】
- 允许：verify-service/src/main/java/com/sportverify/verify/**（含新增 RuleCacheService、TwoLevelCacheProperties、
  RuleNacosConfigListener、RuleCacheInvalidationListener、RuleVersionService、VerifyApplication）
- 允许：verify-service/src/main/resources/application.yml
- 允许：verify-service/src/test/java/com/sportverify/verify/**
- 允许：新增 docs/adr/0008-二级缓存.md；更新 docs/项目速览手册.md、docs/微服务面试项目方案.md、README.md
  （仅限与缓存口径相关的段落）
- 允许：spec/changes/add-two-level-cache/**、spec/specs/sport-record-verify/spec.md（仅第 8 步归档用）
- 禁止：改动 record-service 任何配置（坑 1：record-service 永远不加 yml）
- 禁止：改动 user-service / gateway-service / leaderboard-service / mapmatch-service 的业务代码
- 禁止：触碰 docs/perf/** 的历史压测数据（复测新数据放 docs/perf/data/ 下新文件）
- 声明：无其他写入者。需越界时停下来上报。

【必读材料】
1. spec/changes/add-two-level-cache/proposal.md、tasks.json
2. spec/changes/add-two-level-cache/specs/sport-record-verify/spec-delta.md（**5 需求 / 7 场景**，纯 ADDED）
3. docs/adr/0008-二级缓存.md（该分支已写好，确认合并后没被覆盖）
4. docs/项目指导-后续任务与风险指导意见.md 的「决策 2」「决策 3」「R2」

【关键现状（实测，别按旧文档猜）】
- 该分支对 main **0 处硬冲突**，**不需要 rebase**，直接 --no-ff merge 即可（保住原 4 个 commit）。
- 但 2 个文件两侧都改过，需人工确认自动合并结果：
  · verify-service/src/main/resources/application.yml → **必须确认两边的配置块都在**（阈值分类型的配置 + 缓存的 TTL/前缀配置）
  · docs/项目速览手册.md → 确认缓存段落与类型阈值段落都在
- ⚠️ **文本能自动合并 ≠ 能编译**：该分支 fork 点在 sport-type 之前（其 VerifyEngine 里 sportType 出现 0 次），
  而 sport-type 把 VerifyProperties 的阈值结构改成了按类型嵌套。合并后若编译报错，只修 verify-service 内与缓存相关的
  编译问题（**把缓存读写适配到新的嵌套结构，不得把结构改回扁平**），最多修 1 次。
- ⚠️ **唯一高危点**：RuleCacheService 用 Jackson `writeValueAsString` / `readValue(json, type)` 把 VerifyProperties
  写进 Redis L2；而 readRedis 的实现是 `catch (Exception e) → log.warn + return null → 回源 DB`。
  也就是说：序列化/反序列化一旦对不上嵌套结构，L2 会「**静默永不命中**」——服务不报错、测试照样绿、压测数字失实。
  该分支的序列化路径从未见过嵌套阈值，所以这是真实风险，不是理论风险。

【执行步骤】
1. 基线：git log --oneline -1 main；git status --short
2. 提交提案目录（未跟踪，只在工作区，必须在它自己的分支上提交）：
     git checkout feature/add-two-level-cache
     git add spec/changes/add-two-level-cache
     git commit -m "docs(cache): 提案与规范差异入库"
3. 合并（0 硬冲突，无需 rebase）：
     git checkout main
     git merge --no-ff feature/add-two-level-cache
4. 逐项确认自动合并结果：application.yml（两类配置块都在）、docs/项目速览手册.md（两个段落都在）。各给一句结论。
5. 编译（JDK21 显式启动 Maven）。若报错，只修 verify-service 内与缓存相关的编译问题；最多修 1 次。
6. **写 L2 往返断言测试（本任务硬性交付，不可省）**
   - 构造含多运动类型嵌套阈值的 VerifyProperties（至少 RUNNING 与 CYCLING，且两者 R1 阈值必须不同）
   - 经 RuleCacheService 走一遍真实读写（复用该分支已有测试基建）
   - 断言：读回后按 CYCLING 解析出的 R1 阈值 **≠** 按 RUNNING 解析出的，且等于写入前的值
   - 再补一个负向断言：故意让反序列化失败，断言此时不会「悄悄返回默认值」而被上层当成命中的正常值
     （证明静默降级至少可观测，进 warn 日志）
   - 测试方法上用中文 Javadoc 写清「为什么要有这个测试」
7. 跑测试：期望 BUILD SUCCESS，用例数 >=112（P1 后基线），新增测试计入
8. **归档本提案**（按 P3 的机械规则 R1/R3）：
   - 新增 `## 规则二级缓存` 段落（`## 变更历史` 之前），搬入 5 条 ADDED 需求原文
   - 归档清单追加 `- add-two-level-cache（规则二级缓存）`
   - 变更历史追加一条，引用 docs/adr/0008：为什么两级（本地低延迟 + Redis 跨实例共享）、三防护各自的手段
     （空值哨兵防穿透 / Redisson 锁互斥重建防击穿 / TTL 随机抖动防雪崩）、失效广播与短 TTL 的降级取舍、
     Redis 不可用不阻断校验主链路
   - 回写 tasks.json 的 completed/passes 全 true → git mv 到 spec/changes/archive/
   - 主规范需求 +5、场景 +7
9. 冒烟命令只给出、**不许声称已通过**（真机冒烟由用户跑）：
     docker exec sport-verify-redis redis-cli -h host.docker.internal -p 6379 KEYS 'verify:rules:*'
     # 再 GET 出值，确认 JSON 里含 sportType 维度的键
     # 坑 2：必须 host.docker.internal，直接 redis-cli 看到的是空容器

【验收项（DoD，缺一不可）】
1. feature/add-two-level-cache 出现在 git branch --merged main；git branch --no-merged main 为空
2. 编译 BUILD SUCCESS
3. 测试 BUILD SUCCESS，用例数 >=112 且含新增断言测试（贴测试方法名 + 断言那一行）
4. VerifyEngine 里 sportType 仍在（grep -c >=1）；RuleVersionService 仍含缓存读写
5. docs/adr/ 下 0007 与 0008 并存
6. 主规范需求 77→82、场景 163±1 → 170±1（贴前后数值）
7. 归档清单与变更历史各 +1 条；spec/changes/ 下无残留提案，archive 下共 15 个
8. 回报中明确声明「压测复测与真机冒烟未执行，交用户执行」

【commit 计划（保持可独立编译）】
- docs(cache): 提案与规范差异入库
- 保留分支原有 4 个 commit（不改写 message）
- test(cache): L2 序列化往返断言（证明嵌套阈值不丢）
- fix(cache): 适配按类型阈值结构（仅当第 5 步确有编译修复）
- docs(spec): 归档 add-two-level-cache 并合并规则二级缓存能力域
- 合并提交由 git 自动生成

【明确不做】不跑压测；不改 docs/perf/压测报告.md 的历史数字（复测由用户跑、数字由用户确认后另行更新）

【回报格式（≤300 字，不贴代码）】
- 产出：commit hash 列表 / 新增测试文件路径与行数
- 校验：8 条验收项实测结果（编译测试各贴关键 1-3 行；第 3 条贴断言原文；第 4 条贴两处 grep 结论）
- 第 4 步两个交叉文件的合并结论（各一句）
- 未解决项 / 待决策项
- 必附一句：「L2 是否真命中尚未验证，请用户起服务后执行第 9 步命令」
```

---

## 全部完成后的终检（不由子 agent 自证，由你或我独立跑）

```bash
ls -1 spec/changes/                  # 期望：只剩 archive
ls -1 spec/changes/archive/ | wc -l  # 期望：9 + 6 = 15
grep -c "^### Requirement" spec/specs/sport-record-verify/spec.md   # 54 → 82
grep -c "^#### Scenario" spec/specs/sport-record-verify/spec.md     # 120 → 170±1
grep -n "5 个服务\|6 个服务" spec/specs/sport-record-verify/spec.md  # 只应剩 6
grep -c "auth:lock:" user-service/src/main/java/com/sportverify/user/auth/service/AuthService.java
git branch --no-merged main                                          # 期望：空
git status --short                                                   # 期望：干净
```

---

## 你（用户）必须亲自做的 4 件事（AI 不可代劳）

1. **服务冒烟**：`docker compose up -d` 后起 6 服务，跑榜单 / 鉴权 403 / 账号锁定 / R5 四条冒烟。
2. **L2 真命中核验**：`KEYS 'verify:rules:*'` 若为空 → 就是 R2 静默降级发生了，看日志里的「Redis 回填失败 / 读取规则失败」warn。
3. **压测复测 P95**：缓存层是实现变更，不重测会让 README 的压测数字变成不实陈述（诚实原则）。
4. **录屏与前端**（B5-B7）：演示视频、轨迹可视化页、真实测试集拦截率报告 —— 这是「项目能被看见」的唯一路径，优先级高于 add-tracing。
