# TASK-135 总榜缓存真实入口核实与最小修正

## 目标

核实 `leaderboard-service` 总榜缓存的真实调用入口：从 Spring 容器取得代理后的正常控制器入口，连续两次查询同尺寸总榜时，底层 `ZSetOperations.reverseRangeWithScores` 只应调用一次。

若旧实现的代理判别式失败，仅把缓存接到正常总榜入口；不改好友榜缓存与好友服务降级语义。

## 范围

- 阅读并核实：`LeaderboardController`、`LeaderboardService`、`CacheConfig`。
- 通过 Spring 容器中的代理对象验证调用链；不以直接 `new LeaderboardService` 的 Mockito 单测作为缓存生效证据。
- 只修改总榜缓存真实入口及其相关回归测试、本任务 mailbox 记录。

## 硬边界

- 不给好友榜添加 `@Cacheable`，不修改好友榜实现、好友服务 API 或故障空榜语义。
- 不改变缓存 TTL、缓存数据范围或引入新依赖。
- 不 push、不建 PR；只运行相关测试、仓库目标模块验收入口；涉及缓存配置修改时补跑目标静态入口。
- 若旧实现判别式不失败，停止并记录实际底层调用证据，不为满足预设结论改代码。

## 验收判别式

1. 旧实现：Spring 容器正常入口两次同尺寸总榜查询；底层 ZSet 查询次数应为 1。
2. 修正后：上述判别式通过。
3. 好友榜未被缓存：同一 service 代理连续调用好友榜仍逐次调用好友服务；好友服务故障仍返回空榜。
4. 目标模块测试及必要静态入口按仓库现有脚本执行。
