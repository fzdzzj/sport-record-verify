<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sportverify</groupId>
        <artifactId>sport-verify-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <!-- common 模块：统一 Result / 错误码 / 业务异常 / 全局异常处理器 -->
    <artifactId>sport-verify-common</artifactId>
    <name>sport-verify-common</name>

    <dependencies>
        <!-- 全局异常处理器依赖 Spring MVC 注解（@RestControllerAdvice） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- 事务/DAO 异常处理所需：DuplicateKeyException 与 TransactionSystemException 均在 spring-tx 内
             （已对仓内 jar 核过：spring-jdbc 里命中 0）。此前用 starter-jdbc 是把 HikariCP/spring-jdbc
             一起拖进来当通道；starter-aop 则是为已删除的事务日志切面准备的，本模块无使用方。 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
